# AGENTS.md — 專案代理規範

本文件供 AI 代理（如 opencode）在本專案協作時遵循。

## 專案概況

主題：自行車產業研究報告撰寫。
呈現方式：單一 `.html` 檔案，不使用任何外部函式庫（純 HTML + CSS + 原生 SVG）。

## 設計規範（必遵）

1. **配色**：深色底（`#0d1117` 為主），重點色使用橘色（`#ff7a18`），輔以淺橘（`#ffb066`）與青色（`#39c5cf`）做對照。
2. **懸停說明**：所有數據元素需支援滑鼠懸停顯示說明框（使用 `data-tip` 屬性 + 純 CSS `::after/::before` 實作）。
3. **圖例**：頁面右下角需有固定圖例（`.legend-tag`），說明橘色／淺橘／青色／虛線之含義。
4. **風格**：專業、乾淨、具科技感（monospace 數字、發光色塊、卡片陰影、hover 浮起效果）。
5. **限制**：不引入 CDN、框架或函式庫；一律寫在同一個 `.html` 檔內。
6. **語言**：繁體中文。程式碼註解除非必要否則不加。

## 常用指令

無建置工具、無套件管理、無測試框架。驗證方式為直接用瀏覽器開啟 `.html` 檔檢視效果。

## 專案檔案

- `自行車產業研究報告.html` — 目前唯一的成果檔。